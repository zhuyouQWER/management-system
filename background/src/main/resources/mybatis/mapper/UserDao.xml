<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.zhj.dao.UserDao" >
  <select id="QueryUser" resultType="com.zhj.model.User">
  select  *  from  user where cellphone=#{cellphone} and password=#{password}
  </select>
   <insert id="AddUser">

   </insert>
  <insert id="Add">

  </insert>
</mapper>